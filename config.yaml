name: "Habitus"
description: "Behavioral intelligence for Home Assistant — learns patterns, infers situations, detects anomalies."
version: "0.1.0"
slug: "habitus"
init: false
arch:
  - aarch64
  - amd64
  - armv7
homeassistant_api: true
map:
  - homeassistant_config:ro
options:
  log_level: "info"
  scan_interval_hours: 6
schema:
  log_level: "str"
  scan_interval_hours: "int"
