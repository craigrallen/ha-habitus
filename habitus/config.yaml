name: "Habitus"
description: "Behavioral intelligence — learns patterns from HA, detects anomalies, publishes sensors."
version: "0.7.0"
slug: "habitus"
init: false
arch:
  - aarch64
  - amd64
  - armv7
homeassistant_api: true
ingress: true
ingress_port: 8099
panel_icon: mdi:brain
panel_title: Habitus
map:
  - homeassistant_config:ro
  - data:rw
options:
  scan_interval_hours: 6
  days_history: 365
schema:
  scan_interval_hours: int
  days_history: int
